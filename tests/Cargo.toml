[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[lib]
test = false
doctest = false
[dependencies]
client = { path = "../client" }
shared = { path = "../shared" }
loginserver = { path = "../loginserver" }